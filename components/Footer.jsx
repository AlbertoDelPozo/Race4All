import React from "react";
import { Typography } from "@mui/material";

const Footer = () => (
  <footer data-testid="footer">
    <Typography variant="body1" color="primary" data-testid="footer-text">
      Race4All
    </Typography>
  </footer>
);

export default Footer;
